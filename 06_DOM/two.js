// query selector

// generalise selctor of tags

document.querySelector('h1')    // will gives the 1st h1 tag in document
document.querySelectorAll('h1')     // gives an NodeList ( somewhat similar to array ) of all the h1 tags
// foreach work here as a loop 

document.querySelector('#idname')
document.querySelector('.classname')
document.querySelector('input[type="password')
// the query sleector have get attributes similar to css Eg.
document.querySelector('p:first-child')